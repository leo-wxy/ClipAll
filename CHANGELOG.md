# Changelog

所有值得注意的变更都会记录在这里。

## [0.0.12] - 2026-08-23

### Changed

- 收束按 App 取词规则界面与重复 UI 状态代码，并将主 App 编译纳入完整验证门禁。
- 完善插件替换事务、配置迁移和 manifest 校验的一致性，失败时可恢复旧版本。

### Fixed

- 无明确文字证据时不再自动触发复制，避免图片或非文字控件误取词。
- 修复卸载中断可能丢失插件、损坏恢复记录永久阻塞，以及路径或符号链接越界恢复的问题。
- Runner 按完整响应限制结果与日志预算，避免合法输出因总响应超过 256 KB 被宿主拒绝。
- 内置插件自动修复后保留用户原有禁用状态。

## [0.0.11] - 2026-08-16

### Added

- 增加“跟随系统”“浅色”“深色”三态外观，设置窗口与取词浮窗统一切换并持久保存。
- 操作栏页面增加浮窗实时预览，并限制最多固定四个插件能力。

### Changed

- 重做全部设置子页面、插件列表与配置表单，统一浅色/深色配色、排版、控件和交互状态。
- 将原“能力中心”并入插件管理，内置能力、外置插件及固定操作统一从插件与操作栏入口管理。
- 重做取词浮窗的表面、边框、图标、悬停和执行状态，使其与设置窗口共用设计规则。

### Fixed

- 修复关闭最后一个设置窗口后菜单栏应用被 AppKit 正常终止，导致后续取词看似崩溃的问题。
- 修复外观切换读取 `@Published` 旧值，造成选中浅色但窗口仍保持深色的问题。

## [0.0.10] - 2026-08-14

### Added

- 外置插件运行时升级到 v2，可通过只读 `App.getPluginEnv(pluginID)` 获取自身普通配置。
- 浮窗显示期间支持按 Esc 关闭，并在插件列表与详情中区分内置、本地和开发来源。

### Changed

- 时间工具示例迁移到 manifest/runtime v2，处理器只接收选中文字。

### Fixed

- 卸载插件时同步清理普通配置与插件作用域密钥，并在密钥清理失败时终止卸载。

## [0.0.9] - 2026-08-13

### Added

- 在“设置 → 取词”中增加拖选、双击/多击开关和按 App 自动显示规则。

### Fixed

- 兼容取词可完整恢复 AppKit 无法读取的 Qt 私有剪贴板类型，选中文字继续只在内存中处理。
- 捕获期间前台 App 发生切换时立即拒绝旧请求，避免读取或显示新 App 的选区。

## [0.0.8] - 2026-08-11

### Fixed

- 修复点击搜索等外部能力后浮窗不会立即消失、关闭前位置上抬的问题。
- 修复双击 IDE 文件树、Tab 等非文字对象时误弹浮窗，同时保留真实文字选区的自动取词。
- 支持 POPO 等不暴露鼠标命中 AX 节点的 App，在明确双击选词后通过受约束的内存剪贴板回退取词。

## [0.0.7] - 2026-08-11

### Fixed

- 修复 GitHub macOS 15 arm64 Runner 创建 DMG 后资源尚未释放就立即校验，导致 Release 整体失败的问题。
- DMG 暂时不可用时仍发布 ZIP 与校验文件，避免标签存在但 Release 为空。

## [0.0.6] - 2026-08-10

### Changed

- 删除无调用的领域字段、单实现协议和单产物工厂，合并重复的悬浮高亮实现。
- 合并验证脚本公共环境，移除无实际断言的 XCTest 冒烟目标。

## [0.0.5] - 2026-08-10

### Added

- 增加 AX 优先、模拟复制兜底的跨 App 兼容取词，支持微信等不暴露 Accessibility 选区但可复制文字的控件。
- 增加兼容取词总开关和按 App 排除名单。

### Fixed

- 兼容取词只响应拖选、双/三击、Shift-click、快捷键或菜单命令，避免普通单击和输入触发旧选区。
- 复制回退完整恢复原剪贴板，并在检测到外部写入时避免覆盖用户的新内容。

## [0.0.4] - 2026-08-10

### Fixed

- 修复菜单栏场景重建时启动任务被取消，导致自动取词监听未启动的问题。
- 为自绘控件和 WebView 增加前台 App 焦点、鼠标命中、祖先选区及 Text Marker 回退，恢复跨 App 浮窗。
- 将自动取词收紧为拖动或双/三击选择手势，普通单击旧高亮不再重复弹窗，键盘选择继续使用注册快捷键。

## [0.0.3] - 2026-08-09

### Added

- 外置插件可在 `routingRules.inputMatchers` 中声明受限的 `dateFormat` 输入格式，无需把插件日期格式硬编码进宿主。
- 时间工具插件 `1.1.0` 支持 `yyyy年M月d日` 和中文日期时间输入。
- GitHub Release 同时提供带 Applications 快捷入口的 DMG、备用 zip 与校验文件。

### Fixed

- 自动取词跳过文本、搜索、日期和时间输入控件，避免编辑时干扰输入。

## [0.0.2] - 2026-08-09

### Added

- 在浮层中加入固定粘贴操作，与复制、搜索、翻译组成默认操作栏。

### Changed

- 重新设计设置窗口、插件管理页面、悬浮操作栏与浅色/深色视觉层级。
- 更新应用图标、菜单栏图标、Dock 展示与菜单栏窗口唤起行为。
- 统一浮层宽度、圆角、悬停反馈、展开定位和复制/粘贴后的关闭行为。

### Fixed

- 移除会干扰来源 App 输入法组合的普通按键监听，修复字母重复输入。
- 修复菜单栏打开设置时窗口不置前，以及重复创建窗口的问题。
- 修复宿主版本更新后时间工具示例插件的兼容性提示。

## [0.0.1] - 2026-08-09

### Added

- 发布 ClipAll 首个公开源码版本。
- 提供复制、搜索、翻译和外置时间工具插件的基础能力。
- 提供 macOS 15 arm64 的 ad-hoc prerelease 构建流程。
